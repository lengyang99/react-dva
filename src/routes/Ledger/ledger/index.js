import React from 'react';
import Forms from './Forms';
import List from './List';
import './index.less';

const Ledger = () => (
  <div className="ledger__global">
    <Forms />
    <List />
  </div>
);

export default Ledger;
